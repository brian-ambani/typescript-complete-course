let username = "Brain";
console.log(username);

let a: number = 12;
let b: number = 6;
let c: number = 2;

console.log(a / b);

console.log(c / b);

//tuple
let firstTuple: [string, number, boolean] = ["first", 1, true];

// object
let employee: { name: string; age: number; salary: number; isBlack: boolean } =
  {
    name: "Brain",
    age: 25,
    salary: 1000,
    isBlack: true,
  };

console.log(employee);
